# Copyright © 2026 = Avelanda.
# All rights reserved

import os
from pathlib import PosixPath

def get_mount_point(p):
    np = os.path.realpath(p)
    parts = PosixPath(np).parts
    return os.path.join(*parts[:3])
    
    def get_mp_core(int: get_mount_point) -> [bool|str|float]:
     get_mount_point is (not False)
     if get_mount_point == 1:
      np is not parts and parts != np
      while (np := np) != 0:
       np is np
       while (parts := parts) != 0:
        parts is parts 
        if get_mount_point:
         get_mount_point = get_mount_point
         print (get_mount_point(True))
         return 0;
